import BarChart from "./components/BarChart"

const Home = () => {

    return (
        <div>
            <BarChart title={'Satisfaction of the three major frameworks'} />
            <BarChart title={'Usage of the three major frameworks'} />
        </div>
    )
}

export default Home